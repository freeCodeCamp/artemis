# Binaries
/bin/
/artemis
*.exe
*.test
*.out

# Go build cache / coverage
*.cover
coverage.out
coverage.html

# Editor / OS
.idea/
.vscode/
.DS_Store

# Local env / runtime
.env
.envrc
# config/sites.yaml is the canonical PR-reviewed source of truth per
# ADR-016 §sites.yaml lifecycle (line 178); committed + PR-reviewed.

# Vendored deps (we use modules)
vendor/

# dossier: internal scratchpad theater tooling
.scratchpad/
