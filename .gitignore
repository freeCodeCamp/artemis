# Binaries
/bin/
/artemis
*.exe
*.test
*.out

# Go build cache / coverage
*.cover
coverage.out
coverage.html

# Editor / OS
.idea/
.vscode/
.DS_Store

# Local env / runtime
.env
.envrc.local
# config/sites.yaml is a dormant cold-start seed (Valkey is the live
# registry post-cutover, artemis@f115198); committed for reference only.

# Vendored deps (we use modules)
vendor/

# dossier: internal scratchpad theater tooling
.scratchpad/
